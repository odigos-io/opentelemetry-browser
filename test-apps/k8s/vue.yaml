apiVersion: apps/v1
kind: Deployment
metadata:
  name: vue-app
  namespace: default
  labels:
    app: vue-app
    browser-otel-test: "true"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: vue-app
  template:
    metadata:
      labels:
        app: vue-app
        browser-otel-test: "true"
    spec:
      containers:
        - name: vue-app
          image: browser-otel-vue:dev
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 80
          readinessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 2
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: vue-app
  namespace: default
  labels:
    app: vue-app
spec:
  selector:
    app: vue-app
  ports:
    - name: http
      port: 80
      targetPort: 80
