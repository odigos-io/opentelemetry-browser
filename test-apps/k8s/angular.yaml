apiVersion: apps/v1
kind: Deployment
metadata:
  name: angular-app
  namespace: default
  labels:
    app: angular-app
    browser-otel-test: "true"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: angular-app
  template:
    metadata:
      labels:
        app: angular-app
        browser-otel-test: "true"
    spec:
      containers:
        - name: angular-app
          image: browser-otel-angular:dev
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 80
          readinessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 2
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: angular-app
  namespace: default
  labels:
    app: angular-app
spec:
  selector:
    app: angular-app
  ports:
    - name: http
      port: 80
      targetPort: 80
