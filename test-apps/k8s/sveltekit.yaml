apiVersion: apps/v1
kind: Deployment
metadata:
  name: sveltekit-app
  namespace: test-apps
  labels:
    app: sveltekit-app
    browser-otel-test: "true"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: sveltekit-app
  template:
    metadata:
      labels:
        app: sveltekit-app
        browser-otel-test: "true"
    spec:
      containers:
        - name: sveltekit-app
          image: browser-otel-sveltekit:dev
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3000
          readinessProbe:
            httpGet:
              path: /
              port: 3000
            initialDelaySeconds: 3
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: sveltekit-app
  namespace: test-apps
  labels:
    app: sveltekit-app
spec:
  selector:
    app: sveltekit-app
  ports:
    - name: http
      port: 3000
      targetPort: 3000
---
# Opt the SSR app into Odigos browser instrumentation. The browser language override makes Odigos
# inject the odigos-browser-proxy sidecar (which serves agent.js and injects the <script> into the
# server-rendered HTML) instead of in-process Node instrumentation.
apiVersion: odigos.io/v1alpha1
kind: Source
metadata:
  name: sveltekit-app-source
  namespace: test-apps
spec:
  containerOverrides:
    - containerName: sveltekit-app
      runtimeInfo:
        containerName: sveltekit-app
        language: browser
  workload:
    kind: Deployment
    name: sveltekit-app
    namespace: test-apps
